'use client';

/**
 * Client-side wrapper for dynamically loaded components
 * Currently acts as a placeholder as Chatbot is disabled per user request.
 */

export function ClientComponents() {
  return null;
}
